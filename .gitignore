node_modules/
.firebase/
firebase-debug.log
*.log
.DS_Store

# Wird beim Cloudflare-Pages-Build aus Variables and secrets erzeugt
# (siehe tools/write-firebase-config.mjs) — gehört nicht ins Repository.
firebase-config.json
